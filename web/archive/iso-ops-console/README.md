# ARCHIVED — do not use

This is the **old isometric ops-console map**. It is not the game.

The live map is **Keeper's Chart**:

- file: `D:\FrontMission-MapLab\chart.html`
- served at: `/chart/` (Play.cmd opens this)

Do not open this folder to "fix the map". Do not merge it into the chart. Do not restore
`web/iso/`, `web/app.js`, or this `index.html` as the player view. City / faction / market
UI that used to live here will be rebuilt on the chart when the user asks — this copy is
only so the history is not lost.
