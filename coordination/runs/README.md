# Managed CLI run records

The coordinator stores per-job structured CLI output and diagnostics under a directory
named for the job ID. Runtime logs are evidence, not authority; job state remains in
`MIGRATION_LEDGER.md`.

No migration CLI run has started yet.
