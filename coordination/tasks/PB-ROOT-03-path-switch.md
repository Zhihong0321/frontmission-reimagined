# Task packet: `PB-ROOT-03` — atomic repository-local chart path switch

## Control

- Status: `ACTIVE`
- Worker: `ROOT`
- Runtime: current Codex coordinator acting in an isolated worker worktree
- Green integration base: `eb5b5a6683b1faa75602a2a9b8ebfe9b8cabc4f9`
  (`PB-ROOT-02` verified; product merge `b10878934bd2e528adc80f64c1224f108a9534c9`)
- Branch: `codex/pb-root-03-path-switch`
- Worktree: `D:\FrontMission-RIMG-worktrees\PB-ROOT-03`
- Canonical ledger: `D:\FrontMission-RIMG\MIGRATION_LEDGER.md`
- Canonical plan: `D:\FrontMission-RIMG\MIGRATION_PLAN.md`
- Read-only sibling: `D:\FrontMission-MapLab`

Do not begin unless this task is `ACTIVE` in the canonical ledger and assigned to ROOT.

## Objective

Perform only the bounded Phase B atomic path-switch transaction:

1. Serve `/chart/` only from repository-local `web/chart/`.
2. Generate `web/chart/world.js` only with repository-local
   `web/chart/make-world.js` and `data/`.
3. Remove both runtime sibling-discovery paths and every fallback to MapLab.
4. Make every missing or failed required generation condition fatal.
5. Prove browser and clean-clone provenance using the existing repository-local
   `world.js` header and deterministic bytes.

No other product, documentation, migration, cleanup, or phase work is authorized.

## Required base identity

Before editing, verify all of the following and stop if any differs unexpectedly:

1. The assigned branch descends from integration commit
   `eb5b5a6683b1faa75602a2a9b8ebfe9b8cabc4f9`.
2. Product merges `b10878934bd2e528adc80f64c1224f108a9534c9` and
   `ec7cc79f88b423f9af25acafb78b28e1618264b6` remain ancestors.
3. `D:\FrontMission-MapLab` remains on `backup/maplab-final-20260903` at
   `df3c1baa8a83c2412607353af9994170b988dbe3`, with exactly its previously authorized
   tracked `world.js` delta.
4. Pinned starting files:
   - `src/MechaTrader.Host/Program.cs`: 12,461 bytes, SHA-256
     `1be69355ce8876d54be944124765f2ed31bc3bb3c43cade6e58a6a1d7dffd7cb`
   - `play.ps1`: 4,621 bytes, SHA-256
     `06277e5d6bbefb4b6940828313ff882f0e7ab9f94d9282e7113780f34fb549d5`
   - `tests/browser/smoke.test.js`: 11,945 bytes, SHA-256
     `9c1098e1316df0f076df0263c7c7739252bc552753e96fdb90770b1721260794`
   - `tools/clean-clone-check.ps1`: 5,933 bytes, SHA-256
     `789b6132a0b3f80f9c171d79f32bfe66024447053ba494cdb26a1068644956d9`
5. Repository generator/output remain:
   - `web/chart/make-world.js`: SHA-256
     `9e34b1de203d51f4bc3332d1ab8536734c343af21adc12c423da9091918fe6a0`
   - `web/chart/world.js`: 8,587 bytes, SHA-256
     `26063b3e3680a190b79843604107977331922c77397dfe2a1bf23a5a3160712a`

## Exact allowed write scope

- `src/MechaTrader.Host/Program.cs`
- `play.ps1`
- `tests/browser/smoke.test.js`
- `tools/clean-clone-check.ps1`
- `coordination/handoffs/PB-ROOT-03.md`

## Required implementation

### Host

- Derive the chart root only as `web/chart/` below the already resolved repository web
  root.
- Fail startup clearly if required consolidated chart files are absent.
- Mount `/chart` with `chart.html` as its default document using only that chart root.
- Remove `LocateMapLab`, its parent walk, nullable fallback behavior, and every host-side
  `FrontMission-MapLab` reference.
- Preserve all other host routes, middleware, APIs, and behavior.

### Launcher

- Keep `Update-ChartData`, but resolve only these paths below `$PSScriptRoot`:
  `web/chart/make-world.js`, `data/`, and `web/chart/world.js`.
- Fail the launcher before build or browser startup if Node.js, the generator, data
  directory/required inputs, or generated output is missing, or if Node exits non-zero.
- Never warn-and-continue with stale chart data.
- Remove the parent walk, `FrontMission-MapLab` discovery, and every sibling fallback.
- Preserve the launcher lifecycle outside chart generation.

### Provenance and clean clone

- Do not add a new frontend marker. The existing exact first line
  `// GENERATED by make-world.js from repository-local data/ — do not hand-edit.` and
  full SHA-256 `26063b3e...0712a` are the approved provenance evidence.
- Extend the browser smoke to fetch `/chart/world.js` and assert the repository-local
  header and exact deterministic bytes before accepting the page.
- Update `tools/clean-clone-check.ps1` to the Phase B state: in a full-history clone with
  no reachable sibling MapLab or foreign ancestor `data/config.json`, regenerate and
  verify repository-local `world.js`, run the complete nine-gate `check.ps1`, install/run
  the browser smoke, require repository-local provenance, verify launcher failure for
  representative missing-generator/broken-input conditions before build/browser work,
  confirm no unexpected tracked diff, and clean every process, port, dependency folder,
  log, and temporary clone it creates.
- The clean-clone check must remain Windows PowerShell 5.1 compatible.

## Prohibited write scope

- `D:\FrontMission-MapLab\**` — no filesystem or Git mutation
- `web/chart/**`, including generator/output/frontend bytes
- `data/**`, `check.ps1`, every file below `src/` other than the assigned `Program.cs`,
  every browser file other than `smoke.test.js`, and every tool other than the assigned
  clean-clone check
- `README.md`, `CLAUDE.md`, `MIGRATION_PLAN.md`, `MIGRATION_LEDGER.md`, and every
  coordination file except this job's handoff
- Any deletion, move, rename, gameplay/content change, backend/frontend refactor, tag,
  other Phase B job, or Phase C-F work

## Required worker checks

1. Pinned source/base identity and ancestry checks above.
2. PowerShell parser/syntax check for `play.ps1` and `tools/clean-clone-check.ps1`.
3. `node --check tests/browser/smoke.test.js`.
4. `dotnet build MechaTrader.sln -c Release --nologo -v q` with zero warnings/errors.
5. `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\verify-worldjs.ps1`.
6. Full `powershell -NoProfile -ExecutionPolicy Bypass -File .\check.ps1`.
7. `npm ci --prefix tests/browser`, Playwright Chromium availability, and
   `npm test --prefix tests/browser` with the provenance assertion green.
8. After committing the implementation, run the updated full-history
   `tools/clean-clone-check.ps1`; it must prove successful repository-local generation,
   full acceptance, browser/provenance, no sibling/foreign-data reachability, fatal
   representative launcher failures, clean process/port cleanup, and no unexpected diff.
9. `rg`/diff review showing no runtime `FrontMission-MapLab`, `LocateMapLab`, sibling
   walk, or stale-data continuation remains in `Program.cs` or `play.ps1`.
10. Before/after hashes showing `data/`, repository generator/output, and the MapLab
    checkout remain unchanged.
11. `git diff --check` and exact commit-scope review.

`check.ps1` and the browser suite are separate required gates. Do not claim one includes
the other.

## Stop conditions

Stop `BLOCKED` without expanding scope if:

- Any pinned base/source identity differs unexpectedly.
- Correct serving or fatal generation requires changing frontend, generator, WORLD data,
  input data, unrelated host/launcher behavior, or another prohibited path.
- Browser provenance cannot distinguish the repository-local chart from the untouched
  sibling using the existing generated marker/bytes.
- Any required check remains red after two focused repair attempts.

## Deliverables

- One bounded implementation commit on `codex/pb-root-03-path-switch`.
- Structured handoff at `coordination/handoffs/PB-ROOT-03.md`.
- No sibling mutation, generated WORLD change, data change, refactor, deletion, tag,
  subsequent Phase B job, or Phase C-F work.
